# 2026-09-20 Work Log

## GitHub 发布：作品集网站上传到 GitHub

- **Task:** 将本地 AIGC 作品集网站（C:\gerenwangye）发布到 GitHub。
- **前置状态:** 项目无 git 仓库；gh CLI 未安装；git 全局已配置 user 17717182674m-art / 17717182674m@gmail.com。
- **步骤:**
  1. `winget install GitHub.cli`（v2.101.0）。
  2. `git init` + 创建 `.gitignore`（node_modules/dist）+ 首次提交，分支改名 main。
  3. gh 设备码认证两次失败：用户浏览器能开 GitHub 但命令行直连 `github.com/login/oauth/access_token` 超时 —— 典型「浏览器走代理、终端直连」。
  4. `netstat` 检测到本机代理 **127.0.0.1:7890**（Clash 端口），给 gh 设置 `HTTPS_PROXY=http://127.0.0.1:7890` 后设备码认证成功，登录账号 **17717182674m-art**。
  5. `gh repo create aigc-portfolio --public --source=. --push`（带代理）→ 推送成功。
- **结果:** https://github.com/17717182674m-art/aigc-portfolio （PUBLIC，main 分支）。
- **Lesson:**
  - 本机 gh 安装后需用完整路径 `/c/Program Files/GitHub CLI/gh.exe`（PATH 未刷新）。
  - 该机器访问 GitHub 必须带 `HTTPS_PROXY=http://127.0.0.1:7890`（所有 gh/git 远程操作都要加，除非用户改了代理配置）。
  - gh auth login 后台运行时输出重定向到文件再读取（TaskOutput 工具在部分会话不可用）。
